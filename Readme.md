# 日常刷leetcode
之前这个仓库是leetcode.cn的，有一些代码，有一个commonlib用来获取输入的，并且为了学习使用cmake

现在想重新搞一下，发现自己的算法功底实在是差的要命，各种easy题目做的也很费劲，还是得抓起来

由于是5年前的工程了，cmake也忘得一干二净了，所以删库重来

# 为什么不在网页上直接写
1. 深度vim用户，网页上的操作还是有点麻烦，虽然也提供了vim的模式，但是只是简单支持了hjkl,很多命令都没有

2. 可以调试，可以记录自己的一些想法

3. 每个目录下可以放多个方法，每次都新写一个，看看是否有进步

4. 可以使用不同的语音实现

# TODO
1. 实现测试案例的输入，所以要支持List, Array, String等 
2. 是否集成gtest
